rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tracker_meta/{docId} {
      allow read, write: if true;
    }

    match /sheets/{sheetId}/rows/{rowId} {
      allow read, write: if true;
    }
  }
}
